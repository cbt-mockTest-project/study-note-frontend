import React from "react";
import styled from "styled-components";

const StudyModeToolBoxBlock = styled.div``;

interface StudyModeToolBoxProps {}

const StudyModeToolBox: React.FC<StudyModeToolBoxProps> = () => {
  return (
    <StudyModeToolBoxBlock>
      {/* 컴포넌트의 내용을 여기에 작성하세요. */}
    </StudyModeToolBoxBlock>
  );
};

export default StudyModeToolBox;
